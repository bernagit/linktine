meta {
  name: login
  type: http
  seq: 1
}

post {
  url: {{base_url}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "identifier": "admin@example.com",
    "password": "admin123"
  }
}

settings {
  encodeUrl: true
}
