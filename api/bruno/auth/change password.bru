meta {
  name: change password
  type: http
  seq: 6
}

patch {
  url: {{base_url}}/auth/change-password
  body: json
  auth: inherit
}

body:json {
  {
    "currentPassword": "admin123",
    "newPassword": "Sexyberna-01"
  }
}

settings {
  encodeUrl: true
}
